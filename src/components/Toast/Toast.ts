import styled from '@emotion/styled';

export const ToastStyled = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  z-index: 9999;
  padding: 1rem;
  margin: 1rem;
  border-radius: 0.5rem;
  color: #000;
  max-width: 300px;
`;
